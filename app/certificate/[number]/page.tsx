import {db} from "@/lib/db";import {notFound} from "next/navigation";import Image from "next/image";
export const dynamic='force-dynamic';export default async function Certificate({params}:{params:Promise<{number:string}>}){const {number}=await params;const [c]=await db()`SELECT c.certificate_number,c.issued_at,p.full_name,o.name organization_name,tv.title,tv.version_key,aa.score FROM academy_certificates c JOIN academy_assignments a ON a.id=c.assignment_id JOIN academy_profiles p ON p.id=a.profile_id JOIN academy_licenses l ON l.id=a.license_id JOIN academy_organizations o ON o.id=l.organization_id JOIN academy_training_versions tv ON tv.id=a.training_version_id LEFT JOIN LATERAL (SELECT score FROM academy_assessment_attempts WHERE assignment_id=a.id AND passed=true ORDER BY completed_at DESC LIMIT 1) aa ON true WHERE c.certificate_number=${number} AND c.status='ACTIVE'`;if(!c)notFound();return <main className="certificate-page"><section className="certificate"><Image src="/assets/tax-compliance-pro-logo.webp" alt="Tax Compliance Pro" width={220} height={70}/><p className="eyebrow">CERTIFICATE OF COMPLETION</p><h1>{c.full_name}</h1><p>has completed</p><h2>{c.title}</h2><div className="cert-meta"><span><b>Tax office</b>{c.organization_name}</span><span><b>Training version</b>{c.version_key}</span><span><b>Assessment score</b>{c.score}%</span><span><b>Completion date</b>{new Date(c.issued_at).toLocaleDateString()}</span></div><strong className="cert-number">{c.certificate_number}</strong><small>This certificate documents completion of a Tax Compliance Pro educational program. It does not represent IRS certification, licensing, endorsement, or a determination that the participant has satisfied every legal requirement.</small></section></main>}
