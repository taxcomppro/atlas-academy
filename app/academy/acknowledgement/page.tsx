"use client";import {useState} from "react";import Link from "next/link";
export default function Acknowledgement(){const [name,setName]=useState(''),[accepted,setAccepted]=useState(false),[cert,setCert]=useState(''),[error,setError]=useState('');async function sign(){const r=await fetch('/api/academy/acknowledgement',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({signerName:name,accepted})}),b=await r.json();if(!r.ok)setError(b.error);else setCert(b.certificateNumber)}return <main className="activity-page"><Link href="/academy">← Training Center</Link><p className="eyebrow">ELECTRONIC ACKNOWLEDGMENT</p><h1>Complete your training record</h1>{!cert?<div className="activity-card acknowledgment"><p>I acknowledge that I completed The Staff&apos;s Audit Ready Due Diligence Playbook training. I understand that completing Form 8867 alone does not satisfy all paid-preparer due-diligence requirements.</p><p>I agree to follow office procedures, ask reasonable additional questions, document client responses, escalate unresolved inconsistencies, protect taxpayer information, and refuse to prepare or file information I know—or have reason to know—is false.</p><label className="check"><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)}/>I have read and agree to this acknowledgment.</label><label>Electronic signature<input value={name} onChange={e=>setName(e.target.value)} placeholder="Type your full legal name"/></label><button className="gold" onClick={sign}>Sign and complete training</button>{error&&<p className="error-message">{error}</p>}</div>:<div className="result-card pass"><strong>✓</strong><h2>Training completed</h2><p>Your certificate number is {cert}.</p><Link className="gold link-button" href={`/certificate/${cert}`}>View certificate</Link></div>}</main>}
